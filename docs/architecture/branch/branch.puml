@startuml
title kitcat branch command - Activity Diagram

start

:User runs `kitcat branch [args]`;

if (Are arguments provided?) then (No)

  :Read `.kitcat/refs/heads/` directory;
  :Iterate through branch files;
  :Identify current branch using HEAD;
  :Print branch list (highlight current);

else (Yes)

  :Resolve HEAD commit hash;
  :Create new file in
  `.kitcat/refs/heads/<branch-name>`;
  :Write commit hash into file;

endif

stop
@enduml
